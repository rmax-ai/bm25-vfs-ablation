# Live-model runs only (operator-gated). Tests and the smoke workflow use the mock model
# and need no environment. These are the only variables substituted into configs
# (PLAN.md §0.1); absent values leave the YAML default unchanged. Never commit real keys.
BM25_VFS_BASE_URL=
BM25_VFS_API_KEY=
BM25_VFS_MODEL=
