# Real-model runs only (operator-gated; tests and smoke use the mock model and need no env).
# Only these three variables are substituted into configs (see PLAN.md §0.1); absent values
# leave the YAML value unchanged. Never commit real keys.
BM25_VFS_BASE_URL=
BM25_VFS_API_KEY=
BM25_VFS_MODEL=
