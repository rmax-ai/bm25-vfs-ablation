# Real-model runs (operator-gated later; tests and smoke use the mock model and need no env).
# Names are provisional until PLAN.md §0 pins the config schema.
BM25VFS_BASE_URL=
BM25VFS_API_KEY=
BM25VFS_MODEL=
